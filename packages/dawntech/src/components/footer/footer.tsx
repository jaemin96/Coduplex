type DTfooterProps = {};

const DTfooter: React.FC<DTfooterProps> = (props) => {
  /* === state === */

  /* === handler === */

  return (
    <>
      <div id={"footer"}>footer</div>
    </>
  );
};

export default DTfooter;
