type DTheaderProps = {};

const DTheader: React.FC<DTheaderProps> = (props) => {
  /* === state === */

  /* === handler === */

  return (
    <>
      <div id={"header"}>header</div>
    </>
  );
};

export default DTheader;
